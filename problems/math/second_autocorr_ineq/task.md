# Second Autocorrelation Inequality

AlphaEvolve Appendix B.2. Construct a non-negative step function
`f` to improve the lower bound on a constant C2 related to the
norm of the autoconvolution of `f`.

The program must expose `run()` returning
`(f_values, c2_achieved_from_opt, loss, n_points)`.

Reward = `c2_verified / BENCHMARK`. Maximize.
