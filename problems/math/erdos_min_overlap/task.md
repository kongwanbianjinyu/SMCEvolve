# Erdős Minimum Overlap Problem

AlphaEvolve Appendix B.5. Construct a non-negative step function
`h` satisfying the problem's regularity constraints to improve an
upper bound on the constant governing the asymptotics of the
Minimum Overlap Problem.

The program must expose `run()` returning
`(h_values, c5_bound, n_points)`.

Reward = `BENCHMARK / c5_bound`. Maximize.
